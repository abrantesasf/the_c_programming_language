#include <stdio.h>

int main(void)
{
    // A sentença IF-ELSE é utilizada para expressar decisões. A sintaxe é a
    // seguinte (a parte else é opcional):
    //     if (expressão)
    //         sentença1
    //     else
    //         sentença2

    // ATENÇÃO: a parte ELSE é sempre associada ao último IF. Cuidado com
    // ambigüidades em IF-ELSE aninhados. Use chaves para tornar claro.
    
    return 0;
}
